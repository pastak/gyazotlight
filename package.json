{
  "name": "gyazotlight",
  "version": "0.0.6",
  "description": "",
  "main": "init.js",
  "scripts": {
    "build": "./script/compile && electron-packager ./ Gyazotlight --platform=all --arch=all --version=0.33.3 --prune --asar --out=build --ignore=build --overwrite",
    "build-mac": "./script/compile && electron-packager ./ Gyazotlight --platform=darwin --arch=x64 --version=0.33.3 --prune --asar  --out=build --ignore=build --overwrite",
    "build-linux": "./script/compile && electron-packager ./ Gyazotlight --platform=linux --arch=x64 --version=0.33.3 --prune --asar  --out=build --ignore=build --overwrite"
  },
  "author": "",
  "license": "MIT",
  "compileCacheDir": "./compile-cache",
  "dependencies": {
    "electron-compile": "^1.0.0",
    "electron-packager": "^5.1.0",
    "mkdirp": "^0.5.1",
    "node-localstorage": "^0.5.2",
    "nodejs-websocket": "^1.4.0",
    "react": "^0.13.3",
    "request-promise": "^0.4.2",
    "resolve-path": "^1.3.0",
    "yargs": "^3.26.0"
  },
  "devDependencies": {
    "electron-compilers": "^1.0.0",
    "electron-prebuilt": "^0.26.0"
  }
}
